# DSource Material Bank — production deps (pinned to the dev env).
# torch is CPU-only on the VPS: install via the cpu wheel index (see bootstrap.sh).
curl_cffi==0.15.0
pydantic==2.13.4
numpy==2.5.1
pillow==12.3.0
pdfplumber==0.11.10
open_clip_torch==3.3.0
transformers==5.13.0
sqlite-vec==0.1.9
fastapi==0.139.0
uvicorn[standard]==0.50.0
playwright==1.61.0
